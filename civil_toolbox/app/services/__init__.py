"""Application service layer (scaffold only in Phase A).

See README.md in this directory for the layering rule. Phase A intentionally
ships this package empty: the directory and the rule exist so Phase B has a
home for orchestration without re-litigating where it belongs.
"""
